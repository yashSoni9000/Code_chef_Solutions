#include <iostream>
#include <math.h>
using namespace std;
int main()
{
   int t;
   cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        cout << int(sqrt(n)) << endl;
    }

   return 0;
}


// we only need to imply the prebuilt function to solve this question