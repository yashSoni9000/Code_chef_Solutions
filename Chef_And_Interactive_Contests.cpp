int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int t=1,n,r,temp;
// 	cin>>t;
	while(t--)
	{
	    cin>>n>>r;
	    while(n--){
	        cin>>temp;
	        if(temp>=r) cout<<"Good boi"<<e;
	        else cout<<"Bad boi"<<e;
	    }
	}
	return 0;
}

// we take the input and check if the given number is greater than r we print good boi else print bad boi