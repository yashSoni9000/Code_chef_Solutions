public static void main(String[] args) throws java.lang.Exception
{
    FastReader sc = new FastReader();
    int t = 1, a, b;
    // t = sc.nextInt();
    while (t-- > 0) {
        // Map < Integer, Integer > map = new HashMap < > ();
        a = sc.nextInt();
        b = sc.nextInt();
        sout(a + b + "" + 1);
    }
}

// we add the given 2 numbers and add 1 as string and in order to do that you can simply put "" in between which will convert
// anyting ahead to string