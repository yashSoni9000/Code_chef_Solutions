#define ll long long
#define f(end) for (int i = 0; i < end; i++)
#define e '\n'
#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t, n;
    cin >> t;
    while (t--)
    {
        cin >> n;
        cout << 1 << " " << n << e;
    }
    return 0;
}

// we need to print 2 numbers ,every number has a 1 factor so we print 1 and the number itself